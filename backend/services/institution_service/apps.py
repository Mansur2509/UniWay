from django.apps import AppConfig


class InstitutionServiceConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "services.institution_service"
    label = "institution_service"
    verbose_name = "School / enterprise dashboard"
