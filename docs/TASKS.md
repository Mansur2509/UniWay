# Tasks

## Phase 0 — Foundation

- [x] P0-001 Create repository rules and core documentation.
- [x] P0-002 Create monorepo and local environment structure.
- [x] P0-003 Create Next.js, TypeScript, Tailwind, and FSD skeleton.
- [x] P0-004 Create academic design tokens and placeholder product routes.
- [x] P0-005 Create Django/DRF modular service skeleton.
- [x] P0-006 Add initial user, profile, subscription, university, event, and exam models.
- [x] P0-007 Add API routing, serializers, permissions, and admin foundations.
- [x] P0-008 Add PostgreSQL and Docker Compose configuration.
- [x] P0-009 Add environment examples and demo seed command.
- [ ] P0-010 Run frontend dependency install, lint, and type checks. Blocked on 2026-06-22 by npm registry TLS/network errors (`ECONNRESET` and `UNABLE_TO_VERIFY_LEAF_SIGNATURE`).
- [x] P0-011 Generate backend migrations; run Django checks, migrations, seed, Ruff, and tests.
- [ ] P0-012 Validate Docker Compose startup.

P0-012 requires Docker, which was not installed in the local environment on 2026-06-22. See the latest status in the Phase 0 handoff.

## Phase 1 — Core vertical slices

- [x] **AUTH-001** Implement basic JWT registration, login, refresh, blacklist logout, current-user/profile API, frontend auth state, protected pages, and auth tests.
- [x] **UI-SYSTEM-001** Add semantic dark-first theme tokens and migrate shared/auth/profile/dashboard UI.
- [x] **I18N-001** Add dependency-free typed dictionaries, locale persistence, four locales, and a language switcher.
- [ ] **AUTH-002** Add email verification, password reset, HttpOnly refresh cookies, session/device management, and production auth hardening.
- [x] **PROFILE-001** Implement the self-only student profile aggregate, academic/contact fields, computed completion, localized responsive UI, migration, and tests.
- [x] **PROFILE-STRUCTURED-001** Implement 7 structured admissions profile item types (Activities, Honors, Olympiads, Sports, Research Projects, Essay Drafts, Portfolio Projects) with full CRUD APIs, permission-based access control, reusable frontend components, 4-language i18n, and comprehensive test coverage. All verifications passed: 53/53 backend tests, TypeScript type checks, ESLint linting, Next.js production build, and i18n parity (984 keys across 4 locales).
- [x] **EVENTS-001** Implement event lifecycle, public catalog/detail, profile-snapshot registration, cancellation, my events, localized UI, seed data, migration, and tests.
- [ ] **EVENTS-002** Build Leaflet map/calendar views, save action, and richer eligibility/recommendation filters.
- [x] **UNI-001 / UNIVERSITY-DATABASE-001 / ADMISSIONS-CALCULATOR-001** Implement university catalog search/filter, detail pages, 2-4-way comparison, self-only shortlist, and an admissions fit analysis (Reach/Competitive/Target/Safety) computed only from verified profile and university data, with "Not verified yet" honest empty states everywhere a statistic is unconfirmed. Backend: extended `University` model with nullable admissions fields, `SavedUniversity` shortlist model, `fit`/`shortlist`/`compare` actions on `UniversityViewSet`, pure fit-calculation helpers in `services.py`, 8 fictional demo universities with deliberately partial seed data. Frontend: `/universities`, `/universities/[slug]`, `/universities/compare` routes. All verifications passed: 84/84 backend tests, ruff clean, TypeScript clean, ESLint clean, Next.js build (24 routes), i18n parity (1059 keys x 4 locales). Manually verified end-to-end in the local beta (search/filter, detail, shortlist persistence across refresh, 2- and 4-way compare, fit analysis, no horizontal overflow or double scrollbar at 390px width). Found and fixed a floating-point boundary bug in the GPA-difference threshold check during manual QA.
- [x] **UNIVERSITY-DATA-VERIFICATION-001** Replace the fictional-only catalog with 15 real, source-backed universities (Penn, Princeton, Cornell, CMU, NYU, MIT, Stanford, Harvard, Toronto, UBC, Oxford, Cambridge, Bocconi, NUS, KAIST), each researched via direct official-page fetches/search-result relay (never invented). Added `is_demo` to `University` (default catalog list now excludes the 8 fictional demo records; `?include_demo=true` opts back in) and a new `UniversityFieldVerification` model giving every non-null admissions/stat/cost/deadline field a `source_url`, `last_verified_date`, and `verified`/`partial`/`estimated` status, enforced by a seed-data integrity test. Added `admissions_url`, `financial_aid_url`, `application_portal_url`, `test_policy`, `ielts_minimum`, `sat_p25`/`sat_p75`, `essay_requirements`, `qs_ranking`/`qs_ranking_year` fields. Frontend shows a `VerifiedStat` badge (Verified/Partial/Estimated) with last-verified date and source link per field, "Not verified yet" for anything unconfirmed, and a "Demo data" badge on fictional records. Fit analysis gained a `limited_data_for_category` next-action so a category assigned from only one verified statistic is never presented as a complete picture. All verifications passed: 94/94 backend tests, ruff clean, `makemigrations --check` clean, TypeScript clean, ESLint clean, Next.js build (24 routes), i18n parity (1079 keys x 4 locales). Manually verified end-to-end: real universities appear by default and demo ones are hidden until opted in, Harvard/MIT/NUS/Stanford each render the correct mix of Verified/Partial/"Not verified yet" fields, fit analysis correctly returns "Not enough verified data" for NUS despite its rich non-quantitative data, shortlist and 3-way real-university comparison work, no horizontal overflow or double scrollbar at 390px.
- [x] **ROAD-001 / ROADMAP-GENERATOR-001** Implement a deterministic, rule-based personalized admissions roadmap: profile-gap detection (activities/honors/research/portfolio/essays), exam gaps (SAT/IELTS/AP/planned exams), university deadline countdowns (60/30/7-day + final, sourced from `UniversityFieldVerification` when available), scholarship tasks, fit-analysis-driven weak-dimension tasks, and event-registration reminders. No AI; every task traces to a profile fact, a verified university statistic, or an explicitly-labeled estimated planning window. Backend: new `roadmap_service` app with `RoadmapPlan`/`RoadmapTask`/`RoadmapTaskDependency` models, idempotent `roadmap_generator.py` (stable `dedup_key` per task so regeneration only adds new tasks, never duplicates or deletes), `RoadmapPlanView`/`GenerateRoadmapView`/`RoadmapTaskViewSet` (self-only, manual tasks fully editable/deletable, generated tasks editable except category and never deletable — skip instead) under `/api/roadmap/`. Frontend: full `/roadmap` screen (header, 5 overview cards, category/priority/status/university filters, This week/This month/Later/Completed task board, timeline mode, manual task create/edit form, empty states), a dashboard "next 3 tasks + urgent count" widget, and a "View in roadmap" link on shortlisted university detail pages. All verifications passed: 118/118 backend tests, ruff clean, `makemigrations --check` clean, TypeScript clean, ESLint clean, Next.js build (24 routes, `/roadmap` included), i18n parity (1168 keys x 4 locales). Manually verified end-to-end in the local beta with the seeded demo student: generated 14 tasks from real shortlist/exam-plan/event-registration data, completed and skipped both generated and manual tasks (persisted across refresh), timeline mode sorted correctly, no horizontal overflow or double scrollbar at 390px. Caught and fixed a real bug during QA: `DefaultRouter`'s auto-generated API-root view was silently intercepting `GET /api/roadmap/` ahead of the intended plan view.
- [x] **ESSAY-001 / ESSAY-WORKSPACE-001 / APPLICATION-TRACKER-001 / UNIVERSITY-PROFILE-TABS-002 / DASHBOARD-INTELLIGENCE-002 (EDUVERSE-ADMISSIONS-WORKFLOW-SUITE-001, Phase A)** Implement the essay workspace, application tracker, university detail tabs, and minimal dashboard intelligence. Backend: new `essay_service` app (`EssayWorkspace`/`EssayFeedback`/`EssayRevisionTask`, deterministic `feedback_engine.py` — word count/limit, generic-language, paragraph structure, specificity, why-school/why-major prompt-fit, sentence-length grammar proxy; no AI, no generated/rewritten essay text anywhere) under `/api/essays/`; new `application_service` app (`ApplicationTrackerItem` one-per-university, `ApplicationMilestone` with an optional self-only-validated `linked_roadmap_task`) under `/api/applications/`, status fields never auto-advanced; `roadmap_generator.py` extended with a read-only `_essay_workspace_tasks` rule (no schema change); two new honest-by-default `University` contact fields (`international_office_url`, `virtual_info_session_url`). Frontend: full `/essays` workspace (university/Common-App tabs, draft editor with live word counter, feedback panel, revision checklist — "Get feedback"/"Mark ready"/"Mark submitted" only, no "write for me" button anywhere) replacing the prior beta placeholder; new `/applications` board (Researching -> Shortlisted -> Preparing -> Applying -> Submitted -> Awaiting decision -> Decisions columns, status/priority/sub-status editing, milestones, linked roadmap tasks); university detail page restructured into Overview/Requirements/Essays/Financial Aid/Deadlines/Contact/Sources/Roadmap tabs (Requirements tab reuses the existing fit analysis plus structured-profile item counts for an honest comparison table); 4 new compact dashboard widgets (applications by status, essays by status, next deadline, profile gaps via the existing readiness `improvements` codes) alongside the existing roadmap widget. All verifications passed: 156/156 backend tests (35 new essay/application tests, 3 new roadmap-integration tests), ruff clean, `makemigrations --check` clean, TypeScript clean, ESLint clean, Next.js build (26 routes, `/essays` and `/applications` included), i18n parity (1416 keys x 4 locales). Manually verified against the live local backend/frontend with the seeded demo student: created an essay, generated feedback that correctly flagged generic phrasing and a too-short draft (and only ever returned feedback/checklist items, never essay text), shortlisted and started tracking a Cornell application, regenerated the roadmap and confirmed it produced an `essay_status` task plus the existing university-deadline tasks, verified `linked_university`/`university` filters return matching tasks/applications, added and completed a milestone, and confirmed no forbidden admissions-odds language in the new UI/API/docs.
- [x] **DATA-LOADING-RESILIENCE-001** Fix the reported "broken" data loading on Universities, Event Map, Essays, and Applications. Root-caused empirically (not assumed): the endpoints are healthy — all four return `200` with valid JSON when the backend is warm, CORS is correctly configured (Vercel origin whitelisted, verified via `OPTIONS` preflight), the Events query already has `select_related` + a count annotation (no N+1), and Supabase RLS does not block Django (server-side connection returns full data). The real causes were (1) **Render free-tier cold start** — measured live twice at 64s and >90s for the first request after idle, vs ~0.8s warm — combined with (2) **a frontend fetch layer that had no timeout** (so a cold start hung as an infinite spinner), and (3) **no React error boundary plus a `t()` helper that threw on a missing/dynamic key**, which unmounted the whole route into a blank page (the Essays "blank, no feedback" symptom). Fixes: added an `AbortController` timeout (`REQUEST_TIMEOUT_MS=90s`) to the shared `apiRequest` that converts timeouts/network failures into a typed, retryable `ApiError` instead of hanging; hardened i18n `t()` to fall back to the key instead of throwing; added `app/error.tsx` + `app/global-error.tsx` so no render error can ever produce a blank page; added a `useSlowLoad` hook + `LoadingNotice` "the server may be waking up…" message (after 5s) on the auth/session gate and all four data screens; added a free `.github/workflows/keepalive.yml` that pings `/api/v1/health/` every 10 min to keep the service warm, and documented the cold-start analysis + the Render Starter (~$7/mo, eliminates spin-down) recommendation in `docs/OPERATIONS.md`. Verified: typecheck clean, ESLint clean, i18n parity (1421 keys x 4 locales), Next.js build clean (25 routes), 156/156 backend tests still pass (no backend code changed). Live browser QA (headless preview against the local backend, fixed build): Dashboard + Universities + Essays + Events + Applications all render with real data, **zero console errors or warnings** across the whole session; killing the backend and re-fetching Universities showed the clean "We could not load…" error + working "Try again" (no blank, no infinite spinner) and recovered on retry; the "Include demo data" toggle loads in both states. Production repeated-load timing (warm, 10 runs each, 0 failures): Universities avg 4.1s, Events 3.6s, Essays 3.3s, Applications 3.3s (absolute numbers inflated by the sandbox's network path + a just-woken service; direct single hits and the local browser were ~1-2s/sub-second). Cold-start cannot be eliminated without deploying the fix + keep-alive or upgrading Render; the deployed-cold-start browser test is documented for the user to run post-deploy.
- [x] **DATA-LOADING-RESILIENCE-002** Follow-up after confirming `DATA-LOADING-RESILIENCE-001` was deployed but production still hit Render cold starts. Verified deployment state empirically: GitHub `main` = the fix commit (confirmed via `ls-remote`), and the live Vercel bundle contains the fix's marker strings (`"The server may be waking up"`, error-boundary chunks present) — so there was no deployment gap; the fix is frontend-only (Render needed no redeploy) and all four production endpoints return `200` with valid JSON when warm. The remaining symptom was the cold start itself (the `keepalive.yml` workflow is registered/active but had `total_count: 0` runs — GitHub's scheduled-cron first-run lag). Improvements: lowered `REQUEST_TIMEOUT_MS` to 60s and added a single automatic retry for idempotent GET reads on a timeout/network error (the first failed request triggers the Render wake-up, so the retry usually lands on a warming/warm backend and the page self-heals through a cold start without the user pressing "Try again"). Also fixed the desktop sidebar layout: replaced the `mt-auto` + whole-`<aside>`-scroll footer pattern with a fixed logo + `flex-1 min-h-0 overflow-y-auto` nav region + `shrink-0` pinned footer, which removes the bottom dead-space and lets the nav scroll properly when the list exceeds the viewport (validated by DOM measurement: nav region scrollable, footer pinned at bottom, no gap). Verified: typecheck/ESLint/i18n (1421 keys × 4)/Next.js build all clean; preview console error-free. Known limitation: could not produce real-production *browser* screenshots from this environment (Chrome extension unavailable; preview browser locked to localhost) — production verification was done via direct endpoint probes + deployed-bundle inspection, and the desktop sidebar fix was validated by DOM measurement at a constrained height rather than a desktop screenshot.
- [ ] **PRODUCTION-RESCUE-004** Stabilize live loading contracts and roadmap visibility before redeploy. Local code changes implemented: shared frontend API now rejects non-JSON/invalid JSON and normalizes array or DRF-paginated lists; Universities, Events, Essays, Applications, Roadmap task, Profile item, Organizer, and Moderation list clients use that normalization; Essays and Applications no longer fail the whole page when the secondary shortlist request fails; Roadmap defaults to All tasks, surfaces exam planning windows, treats application tracker items as target universities, and labels SAT/IELTS/AP suggestions as `planning_window` rather than official dates; forbidden admissions-odds wording was removed from active UI/docs/tests except legitimate profile-completion fields. Follow-up layout fix from the screen recording: the desktop app shell is fixed to the viewport at `lg+`, so body no longer creates a second page scrollbar beside the main content scroller. Validation passed locally: 158/158 backend tests, Django check, migration check, local migration, Ruff, i18n parity (1428 keys x 4), TypeScript, ESLint, and Next build (25 routes); follow-up frontend `typecheck`, `lint`, and `build` also pass. Pending: commit/push/deploy confirmation.
- [x] **UNIVERSITIES-API-PATH-HOTFIX-001** Fix the production `/universities` page showing "We could not load the university catalog." Root-caused empirically by inspecting the deployed Vercel bundle: `NEXT_PUBLIC_API_BASE_URL` was set to the bare backend origin `https://eduverse-vvw2.onrender.com` with no `/api/v1` suffix (the bundle literally contained `apiBaseUrl:"https://eduverse-vvw2.onrender.com"` and no `/api/v1` anywhere). Because the DRF router (universities, exams, profiles, subscriptions) is mounted under `/api/v1/`, every `base: "api"` call hit `<origin>/universities/` and 404'd, while auth/events/roadmap/essays/applications kept working because `env.ts` rebuilds those from `new URL(apiBaseUrl).origin` (which discards the path). This is the same `GET /universities/` 404 seen earlier in the Render logs — it was a real frontend misconfiguration, not bot noise. Fix (code-side, so it self-heals regardless of the env value and is defensive against recurrence): `env.ts` now normalizes `apiBaseUrl` — a value already ending in `/api/v1` is kept as-is, anything else is rewritten to `new URL(value).origin + "/api/v1"`. Added dev-only diagnostics to `getUniversitiesRequest` (final request URL, status, message, response body preview; never logs tokens). Response normalization (array vs DRF `{results}`) and the empty-vs-error states were already handled by `normalizePaginatedResponse` + the screen. Verified locally with `.env.local` deliberately set to the broken bare origin: DevTools/preview Network showed the catalog request resolve to `https://eduverse-vvw2.onrender.com/api/v1/universities/` → 200 (not the bare `/universities/`), catalog loaded real data (Stanford, Cambridge, ...), no error state, zero console errors. typecheck/lint/build clean. Recommended (optional, not required now that the code self-heals): also correct the Vercel Production env var to `https://eduverse-vvw2.onrender.com/api/v1`.
- [x] **EDUVERSE-DEPTH-POLISH-001** Deepen the existing admissions workflow without adding broad new modules. Backend: added persistent self-only `suggestions_service` under `/api/suggestions/` with deterministic rules for application/scholarship deadlines (60/30/14/7/final), missing official-data verification, essay word limits and checkpoints, planned exam retakes, SAT/IELTS/AP planning windows, scholarship type checks, course/AP recommendations, profile gaps, and roadmap instructions; suggestions can be dismissed or added to roadmap and never invent official dates or use AI. Roadmap generation now uses 14-day deadline reminders and excludes demo universities/events for normal users. Frontend: added reusable source-aware suggestion panels to Dashboard, Roadmap, Essays, Applications, and University detail; added compact collapsible Roadmap instructions; onboarding now stores planned-retake metadata in `exam_plans`; pricing shows Free Beta + paid plans Upcoming/Coming soon/Not active during beta; dashboard upgrade CTA replaced with "Plans coming soon"; demo events get a visible Demo badge. Logout audit: desktop sidebar, mobile header/nav, organizer/admin shell, and onboarding all expose logout/exit. Docs updated: API contracts, architecture, decisions, and this task log. Validation passed locally: Django check, `makemigrations --check --dry-run`, SQLite migration, 165/165 backend tests, Ruff, i18n parity (1479 keys x 4), TypeScript, ESLint, and Next production build (25 routes). Pending: commit/push and production QA were not run in this local implementation step.
- [x] **UNIVERSITY-DATA-IMPORT-002** Import the real 80-row university XLSX dataset into the catalog and make every field visible/usable. Backend: added `openpyxl`; 7 new `University` fields (`ielts_competitive`, `application_requirements`, `ap_recommendations`, `deadlines_text`, `financial_aid_notes`, `scholarships_text`, `data_quality_notes`) + migration `0005`; testable parser/upsert module `xlsx_import.py` + idempotent management command `import_universities_xlsx` (flags `--dry-run`, `--replace-existing`, `--include-questionable-stats`, `--source-label`, `--default-verification`, `--report`). Normalization without inventing data: acceptance rate fraction/percent → percent number; tuition symbol/country currency (USD-equivalent note when a `$` figure appears on a non-US school); Excel-serial + ISO/`Mon D, YYYY` dates; identical-percentile SAT placeholder detection (dropped + flagged unless `--include-questionable-stats`, then `estimated`); textual GPA kept as a note not forced to float; multi-deadline parse (RD → `application_deadline`) with raw text preserved; majors → programs; scholarship-type detection; multiline source URLs → `UniversityDataSource`; per-field `UniversityFieldVerification`. Idempotent upsert by parenthetical-stripped slug enriches the curated seed rows instead of duplicating and preserves curated verifications. Frontend: detail tabs render all new fields (Requirements: IELTS competitive + application requirements + AP recommendations; Deadlines: raw block; Financial aid: aid notes + raw scholarships; Sources: data-quality notes); +8 i18n keys x 4 locales (1487 total). Verified locally (seed_demo + import = 89 universities, 0 duplicates, 14 seed rows updated / 66 created; re-run idempotent): Django check, `makemigrations --check`, Ruff, full backend test suite (incl. 17 new importer tests), i18n parity, TypeScript, ESLint, Next build all pass; live preview QA confirmed MIT/KCL detail tabs and the `/universities` list render imported data with no admission-odds language. Production import is a one-time `python manage.py import_universities_xlsx ...` run in the Render shell (workbook is committed under `backend/data/universities/`); the command is safe to re-run (upsert, no deletes).
- [x] **EDUVERSE-UNIVERSITY-IMPORT-ADMIN-001** Build an admin-only XLSX upload/import workflow so production university data can be loaded without local `DATABASE_URL`, Render Shell, startup imports, or public endpoints. Backend: added `UniversityImportJob` + migration `0007`, admin/staff-only endpoints under `/api/admin/university-import/` (`dry-run`, `execute`, `jobs/{id}`), shared `load_xlsx_rows()` workbook reader in `xlsx_import.py`, beta-only daemon-thread job execution, `.xlsx` + 10 MB upload validation, secure temp-file handling with best-effort deletion, dry-run rollback, execute atomic import, and job summaries with row/source/deadline/essay/SAT/verification counters. Frontend: added `/admin/university-import` protected by `ProtectedRoute allowedRoles=["admin"]`, admin navigation, upload/dry-run/report/confirm-execute flow, polling job status, warning states, and post-import verification links for catalog/search/detail/Sources tab. Docs updated: API contracts, architecture, data sources, security, decisions, and this task log. Validation passed locally: Django check, `makemigrations --check --dry-run`, SQLite migration, 189/189 backend tests (including 7 new admin import API tests), Ruff, i18n parity (1540 keys x 4), TypeScript, ESLint, Next production build (26 routes), `git diff --check`, and admissions-probability guardrail scan. Pending: push/deploy and production admin import QA were not run.
- [x] **ADMIN-IMPORT-AUTH-GUARD-HOTFIX-001** Fix the admin import production route getting stuck on "Confirming your EduVerse session" after the correct Vercel domain was verified. Root cause: frontend auth storage assumed `window.localStorage` was always readable/writable; if browser storage access was unavailable or threw before the session request, `AuthProvider.refreshUser()` could leave the global gate in `checking` without redirecting or showing a retry state. Fix: centralized auth storage now catches storage failures, clears malformed stored tokens safely, and keeps an in-memory token fallback for the current tab; `AppGate` redirects unauthenticated visits to protected/product URLs back to `/login` instead of leaving them behind the session gate. Importer logic and backend endpoints were not changed; no XLSX upload, dry-run, or import was executed. Validation passed locally: frontend typecheck, ESLint, and Next production build (26 routes, `/admin/university-import` included).
- [x] **EDUVERSE-ADMIN-BOOTSTRAP-001** Grant EduVerse admin access to three real emails (`timarus52111@gmail.com`, `khamidjonovmansurjon@gmail.com`, `iilich6304@gmail.com`) so they can reach the admin-only `/admin/university-import` flow, without touching importer logic, running any XLSX import, or changing frontend design. Admin access is `is_admin_role` = `is_staff or is_superuser or role == "admin"` (`auth_service.User`, used by `IsAdminRole`), so promotion sets `role="admin"` + `is_staff=True` (mirroring how `seed_demo` configures `admin.demo`); passwords are never touched and no users are created. Two idempotent, case-insensitive paths: a `promote_admins --emails ...` management command (reports promoted / already_admin / missing_users / skipped, handles duplicate input and ambiguous multi-match) and a tiny `auth_service` data migration `0002_promote_known_admins` that promotes only the three allow-listed emails **if they already exist** (no-op otherwise, never creates users, never runs heavy work — unlike the XLSX import). Missing accounts are reported, not fatal: register the email, then re-run the command (or redeploy to re-run the migration is not needed — the command is the activation step). Tests (`common/tests/test_promote_admins.py`): normal user promoted, idempotent re-run, missing email reported without crashing, promoted user reaches the admin import endpoint (404 on a missing job id = access granted), student/organizer still blocked (403). Validation: Django check, `makemigrations --check`, full backend test suite, Ruff, frontend TypeScript/ESLint/i18n (1540 x4)/Next build all pass. Activation in production: after the three users register, run `python manage.py promote_admins --emails timarus52111@gmail.com khamidjonovmansurjon@gmail.com iilich6304@gmail.com` (or the data migration promotes any that already exist on the next deploy).
- [x] **URGENT-PROD-STARTUP-SEED-DEMO-HOTFIX-001** Production Render deploys failed with `OperationalError: canceling statement due to statement timeout / while locking tuple in relation "university_service_university"`. Root cause: the start command runs `seed_demo` on every web-service startup, and `seed_demo` performed `update_or_create` over the real/demo university rows (`seed_real_universities`/`seed_university`) — under Supabase lock contention that `SELECT … FOR UPDATE` exceeded the statement timeout and crashed before gunicorn could open its port (same "no heavy work during startup" lesson as the XLSX-import migration). Fix: heavy demo/university/event/exam seeding is now **off by default** in `seed_demo` and gated behind `--with-demo-data`; the default (and the existing `… && seed_demo && gunicorn` start command) now only runs the tiny, university-free admin promotion, so startup never locks university rows. Added a dedicated startup-safe `bootstrap_admins` command and a shared `common/admin_bootstrap.py` (allow-list + `promote_known_admins(user_model)` helper, reused by `bootstrap_admins`, the `seed_demo` hook; `role="admin"` + `is_staff=True`, never creates users or touches universities). `run-local-preview.ps1` now calls `seed_demo --with-demo-data` so local QA still seeds the full dataset. Recommended Render start command (optional, since the default is now safe): `python manage.py migrate --noinput && python manage.py bootstrap_admins && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT`. Tests (`common/tests/test_admin_bootstrap.py`): `bootstrap_admins` promotes an existing allow-listed user, is idempotent, reports missing without crashing and creates no users, a promoted user reaches the admin import endpoint; `seed_demo` default leaves the `University` table untouched while still promoting admins, and `--with-demo-data` does seed universities. Validation: Django check, `makemigrations --check`, full backend suite, Ruff, and frontend TypeScript/ESLint/i18n (1540 x4)/Next build all pass. No XLSX import was executed.
- [x] **ADMIN-UNIVERSITY-IMPORT-DRY-RUN-READONLY-HOTFIX-003** Production admin dry-run failed with `canceling statement due to statement timeout / while updating tuple in relation "university_service_university"`. Root cause: the dry-run ran the *writing* importer inside a rollback transaction, so it still issued `save()`/`get_or_create()`/`update_or_create()` and held row locks on the university tables until rollback — Supabase timed out on those locks. Fix: split `xlsx_import` into a pure `parse_row` plus `plan_import_rows` (a TRUE read-only preflight: parses every row, decides created/updated/skipped via a single bulk `slug__in` SELECT, never calls save/get_or_create/update_or_create, never locks rows, never relies on rollback) and `execute_import_rows` (writes each university in its own short transaction instead of one giant lock-holding transaction, isolates a bad row instead of aborting, deletes nothing). `import_jobs.py` dry-run now calls `plan_import_rows` with no transaction wrapper; execute calls `execute_import_rows` with no outer transaction. Frontend admin screen: the source-URL/skip/SAT warning banner and the metrics grid now render only for a `completed` job — a `failed` run shows the actual error plus a "counters not computed" note (no misleading "missing source URLs" on a zero-row failure), a running job shows a "computing…" note, and Execute stays disabled unless the dry-run completed. Tests: `plan_import_rows` performs no writes (mocked `save`/`get_or_create`/`update_or_create` asserted not called), changes no existing fields, leaves `University.objects.count()` unchanged, still reports created/updated estimates; existing admin-import API tests (admin-only 403/401, dry-run writes nothing, execute writes, duplicate execute no-dup, failed job records error) still pass. Validation: Django check, `makemigrations --check`, full backend suite, Ruff, frontend TypeScript/ESLint/i18n (1542 x4)/Next build all pass. No real import was executed.
- [x] **ADMIN-UNIVERSITY-IMPORT-EXECUTE-STUCK-005** Fix execute jobs that can remain `running` with zero counters and no visible progress if the daemon thread is interrupted or a long row write never reaches final completion. Production inspection of job `7` was read-only: the API still reported `running`/zero counters, while the catalog count had increased to 45 with no duplicate names/slugs, proving partial writes happened even though the job record never updated. Backend: added `processed_count`, `current_row`, `current_university`, and `last_heartbeat_at` to `UniversityImportJob`; the runner now records start/parse/row progress and final heartbeat, logs each row before writing, and marks stale running jobs as failed with an explicit timeout message when an admin reads the job detail. Execute remains per-university short transactions and idempotent upsert; no importer data rules changed. Frontend: running reports now show processed rows, current row/university, last heartbeat, and stale warning instead of a vague final-only "computing" state. Docs updated: API contracts, architecture, data sources, operations, and decisions. No second import was started.
- [x] **EDUVERSE-GLOBAL-PAGINATED-GRID-001** Add a reusable page-based pagination pattern so long item surfaces no longer show only the first backend page. Frontend: added `PaginationControls`, `PaginatedGrid`, and `PaginatedList` shared UI primitives with localized labels; `/universities` and `/events` now request `page_size=21` and preserve backend `count`/page metadata, with filters resetting to page 1; my events, applications, essays, roadmap task buckets/timeline, organizer events, moderation queue, and event participants now expose previous/next/page controls where lists can grow. Backend was unchanged because `DefaultPagination` already supports `page_size` and caps it at 100. Activities remains a placeholder module with no repeated item list yet; admin university import has no job-history list UI yet. No XLSX upload, dry-run, execute, importer logic, or university data changes were made.
- [x] **EDUVERSE-GLOBAL-PAGINATION-AND-SLIDERS-001** Finish the stricter global pagination controls and university catalog query surface. Shared `PaginationControls` now supports `pageSize`, `totalCount`, first/last page controls, page range summaries, and the newer `onPageChange` prop while preserving existing previous/next/page-select callers; `PaginatedGrid`/`PaginatedList` accept `pageSize` and loading states. `/universities` keeps backend `page_size=21`, shows total count, preserves filters across page changes, and adds city, verification-status, QS ranking, tuition, and total-cost sorting/filtering. Essays now paginate as a one-column list of 5 items per page. Existing events/applications/roadmap pagination paths keep using the shared controls without changing roadmap List/Timeline behavior. Backend added only safe catalog filters/orderings (`city`, `verification_status`, `qs_ranking`) with tests. No fake activities grid was invented, no XLSX upload/import/dry-run/execute was performed, and no forbidden admissions-outcome wording was added.
- [x] **EDUVERSE-V2-NORMALIZATION-AND-FIT-FOUNDATION-002** Add the normalization and evidence-based fit foundation for the V2 admissions workflow. Backend: added source-preserving academic fields to `StudentProfile`, deterministic GPA normalization (`4.8/5.0 -> 3.84/4.0`, percentage/IB handling, low-confidence unknown/custom systems), curriculum context, USD cost fields plus `ExchangeRate`, a richer `calculate_university_fit()` response with `fit_score` 1-100, `dream`/`reach`/`competitive`/`target`/`safety` categories, subscores, strict IELTS/SAT gap detection, conditional planned-retake notes, missing-data confidence, and a recommendations endpoint. Added `OfficialExamDate` so SAT/AP roadmap dates can be verified only from College Board sources; otherwise roadmap creates a verification task instead of inventing dates. Frontend: university detail/list/compare/profile/onboarding now show original vs normalized academics, source-aware USD cost context, fit score/confidence/subscores/disclaimers, strict exam risks, sorting by USD tuition/total cost, support link (`https://t.me/Otvet_mne_uje_nakonec`), and reusable `HelpTooltip`. Docs updated: API contracts, decisions, data sources, and this task log. Validation passed locally: Django check, `makemigrations --check --dry-run`, full backend suite (222 tests), Ruff, i18n parity (1642 keys x 4), TypeScript, ESLint, and Next production build (26 routes). No university XLSX upload, dry-run, execute, or importer run was performed.
- [x] **EDUVERSE-ROADMAP-V2-UX-AND-SUGGESTIONS-001** Improve Roadmap and source-aware suggestions usability without changing university import, GPA/currency normalization, fit scoring, or university data. Backend: `RoadmapTask` now preserves `linked_application` when a suggestion is added, reports `task_kind` and `is_timeline_marker`, supports filters for application/source/manual-vs-generated/exam/list-vs-timeline, and keeps generated-task dismissal as skip/history rather than hard-delete. Added tests for idempotent add-to-roadmap, source preservation, persisted dismissals, application/exam/source filters, self-only task behavior, and list/timeline marker separation. Frontend: suggestions now render as a compact one-card carousel by default, group repeated exam-planning items, hide long explanations behind Details, support group dismiss/add controls, and expose Show all/Collapse. Roadmap now has visible List View / Timeline View segmented controls, active/completed/skipped/all status filters, stronger filters, completed tasks in a collapsed section, timeline-only countdown markers, task explanations with what/why/where/source/verification context, and confirmation before delete/dismiss. i18n expanded to 1709 keys across 4 locales. No XLSX upload/import/dry-run/execute was performed, no official dates were invented, and no admissions-probability language was added.
- [x] **EDUVERSE-ESSAY-V2-AND-APPLICATIONS-HOTFIX-001** Small UX hotfix for essays/applications/profile/roadmap. Grounded first: most of the spec was already implemented on `587788c` and left untouched — IELTS decimal input already works (`step=0.5` input; backend `validate_test_scores` accepts float scores and enforces the `(0,9)` range, rejecting 9.5/negative with a clear error — PART 7), essay word-limit display (`wordCountWithLimit`), mark-ready action and `PaginatedList` (PARTS 2–4), essay word-limit suggestions (PART 5), and milestone add/save with empty-title validation (PART 6) all already exist; no university importer, roadmap generator, or fit-score logic was touched, and no XLSX import was run. Filled the genuine remaining gaps: (PART 8) roadmap filters already auto-apply but gave no signal, so added an explicit "Filters" header with a "Filters apply automatically" badge and a live "Showing N task(s)" result count (no horizontal overflow, mobile-safe, List/Timeline toggle unaffected); (PART 6) added a `HelpTooltip` explaining what milestones are next to the Milestones heading, and hardened milestone saving against duplicate-on-double-click by awaiting the submit and disabling the Add button while the request is in flight (empty-title already blocked; owner-scoping already enforced server-side). No admission chance/probability/odds language added; no invented essay prompts or dates. Validation: frontend i18n parity (1713 keys x 4), TypeScript, ESLint, Next build all pass; backend unchanged so `check`/`makemigrations --check`/Ruff clean and the 204/204 suite is unaffected. Deferred (documented, not silently dropped): aggressive multi-prompt essay splitting (PART 1) — kept conservative per the "do not split if uncertain" rule; these are larger follow-ups, not regressions.
- [x] **EDUVERSE-UNIVERSITY-DETAIL-PROGRAMS-AND-GAP-SEVERITY-HOTFIX-001** Fix university-detail program rendering and requirement-gap honesty without touching stored data, importer logic, or the fit API contract, and without adding admissions-odds language. Program display: added a pure `program_display.py` helper that flattens a parent category with parenthetical/comma-split subtracks into clean labels (`"Engineering (Civil, Mechanical, EE, Aerospace, Chemical)"` → `Engineering — Civil` … `Engineering — Electrical Engineering` … `Engineering — Chemical`), strips stray parentheses, carries a parent forward only after an explicit unmatched `(` in the source sequence (no over-inference from unrelated rows), and expands abbreviations only in a safe parent context (`EE`→Electrical Engineering under Engineering; `Business — EE` stays abbreviated). Raw `UniversityProgram.name` is never mutated; the serializer adds per-program `display_name` + top-level `program_display_names[]`, and the detail screen renders those. Gap severity: replaced the binary `on_track`/`gap` with graded, per-metric statuses (IELTS near/moderate/substantial/significant at 0.5/1.0/1.5; SAT close/below/well-below/significant at 50/100/150; GPA on normalized 4.0 only, showing original + normalized + "converted for comparison only"), each with a short badge label and a hover tooltip carrying the concrete numeric gap. Fixed the IELTS *competitive* bug where the row was hard-coded to `on_track` whenever a competitive value existed regardless of the student's score — minimum and competitive rows now run the same comparison, and the UI never shows "On track" below threshold. The same severity thresholds now feed the fit academic subscore (a ~300 SAT gap outweighs a 0.5 IELTS gap) as a weighting refinement with no new fit-response fields; the SAT midpoint branch's prior double `index_shift` was corrected. Added `program_display.py`/`test_program_display.py` and severity/display tests in `test_fit_helpers.py` + `test_universities.py`. i18n: added severity labels + `{{gap}}` help texts across en/ru/uz-Latn/uz-Cyrl. Validation passed locally: Django check, `makemigrations --check --dry-run` (no changes), full backend suite (238 tests), Ruff, i18n parity (1750 keys x 4), TypeScript, ESLint, and Next production build (25 routes). No XLSX upload/import/dry-run/execute was performed and no admissions-probability wording was added. Pending: commit (no push).
- [x] **EDUVERSE-APPLICATIONS-V2-TIMELINE-AND-DEADLINE-INTELLIGENCE-001** Turn Applications into a source-aware planning system without inventing dates, changing university data, or adding admissions-odds language. Audited first: the tracker, milestones, roadmap links, and (critically) `suggestions_service` — which already generates the 60/30/14/7/0 deadline series, source confidence, scholarship/essay/exam windows, and idempotent add-to-roadmap — so the timeline **derives** rather than duplicates (`UniversityDeadline`/`UniversityEssay` don't exist; deadlines live on `University.application_deadline`/`deadlines_text`, `UniversityScholarship.deadline`, and the tracker's date fields). Backend: new pure `application_service/timeline.py` + read-only `GET /api/applications/{id}/timeline/` returning `deadlines[]` (per-kind date + `days_remaining` + `urgency` far/upcoming/soon/urgent/critical/overdue + `confidence` verified/partial/user_provided/estimated/missing + source), `events[]` (chronological), phase-aware `suggested_dates[]` (checkpoints worked back from a real reference deadline, shown only inside their applicable window so a distant deadline never creates urgent work and an imminent one drops long-term tasks), `linked_essays[]` (caller's essays for that university), and `linked_exams[]` (SAT/IELTS/TOEFL/AP with reused gap-severity, planned-retake context, official College Board SAT/AP dates, and a `scores_arrive_before_deadline:false` "may not help this cycle" flag). A missing deadline is surfaced as "Deadline not verified yet" with a verify action, never as safe. `ApplicationMilestone` gained `priority`+`notes` (migration `0002`). Frontend: independent timeline in the application detail (deadline intelligence, next-5 + expand, suggested dates with "why this date?" tooltips, linked essays, linked exams), compact auto-applying filters (priority/round/urgency/missing-deadline) + sort (nearest deadline/urgency/name/recently updated/progress) with a result count, milestone priority/notes in the form and list, dashboard next-deadline urgency + days-remaining + missing-verified-deadline warning, and PART 10 help tooltips. Actionable add-to-roadmap stays on the existing `suggestions_service` (single source of truth). Validation passed locally: Django check, `makemigrations --check --dry-run` (no changes), full backend suite (251 tests incl. 14 new timeline/milestone tests), Ruff, i18n parity (1843 keys x 4), TypeScript, ESLint, and Next production build (25 routes). No XLSX upload/import/dry-run/execute was performed, no official dates were invented, and no admission chance/probability/odds language was added. Pending: commit (no push).
- [x] **EDUVERSE-UNIVERSITY-RECOMMENDATION-ENGINE-001** Build a real 20-25 university recommendation engine grouped by application strategy, reusing rather than duplicating the existing fit-score foundation. Audited first: `/api/v1/universities/recommendations/` already existed with country/region filtering and a raw top-25-by-score list; `calculate_university_fit` already had the ultra-selective "never safety below 10% acceptance" guardrail, planned-retake conditional notes that never boost the current score, normalized GPA/cost context, and fully-translated strength/risk/missing-field/next-action codes — all reused as-is. Rewrote `services/university_service/recommendations.py`: program matching now checks the university's real (ADR-032 display-clean) programs for an exact intended-major match, then a small conservative set of subject clusters (CS/engineering, business/economics, politics/law/IR, biology/pre-med, psychology, social sciences, humanities, arts/design, education, environmental studies, data/AI) for a related match, returning nothing invented when neither matches; a balanced list (5 dream / 7 reach / 8 target / 6 safety, 20-25 total) replaces the flat top-N, with `competitive` folded into `reach` for list purposes only (the `/fit/` endpoint's five categories are unchanged) and "international" as a cross-cutting `is_international` facet rather than a fifth additive bucket; each item adds `deadline_confidence`/`urgency`/`days_remaining` (same thresholds as the ADR-033 application timeline), qualitative `cost_risk` (no invented budget field), `academic_risk`/`profile_risk`/`deadline_risk` labels derived from existing subscores, an `application_round` recommendation parsed from raw deadline text via word-boundary regex and gated on days-remaining plus essay/exam readiness (never inventing an ED/REA policy), and `is_shortlisted`/`application_id` via two bulk lookups (no per-university N+1, verified by a query-count test). Frontend: new `/recommendations` page grouped into Dream/Reach/Target/Safety sections with a compact auto-applying filter bar (category, country, cost risk, deadline urgency, confidence, international-only) and sort (best fit/deadline soon/lowest cost/highest confidence), expandable cards (why recommended, round reason, current-vs-conditional fit, academic/profile/deadline risk, missing data) with idempotent Add to shortlist / Track application / View details actions, plus a compact "Recommended next schools" dashboard widget (top 3, non-blocking on failure). Validation passed locally: Django check, `makemigrations --check --dry-run` (no changes), full backend suite (274 tests incl. 23 new recommendation-engine tests), Ruff, i18n parity (1923 keys x 4), TypeScript, ESLint, and Next production build (27 routes, `/recommendations` included). No XLSX upload/import/dry-run/execute was performed, no official dates or university data were invented, and no admission chance/probability/odds/guarantee language was added (the disclaimer's own negation of "guarantee" is the one sanctioned exception, verified by a dedicated test). Pending: commit (no push).
- [x] **UNIVERSITY-DATA-IMPORT-002 + GLOBAL-UX-CONSISTENCY-001** Audit the existing verified university pipeline and standardize Save/Close behavior without duplicating import logic. The audit found the requested data foundation already exists in the current repo: source-aware XLSX importer (`xlsx_import.py`), admin-only upload/dry-run/execute jobs, job heartbeat/progress, `UniversityFieldVerification`, source/data-quality display fields, display-safe program labels from ADR-032, and the balanced recommendations engine from ADR-034. No new importer, seeder, fake data, upload, dry-run, execute, or university data change was performed. Frontend: added a reusable unsaved-change guard/dialog and applied it to essay/application/roadmap forms, profile structured item forms, the main profile form, onboarding logout/draft save, organizer event draft back navigation, and the essay draft editor's essay-switch/discard flow. Backend: relaxed the old blanket 120-character profile JSON list limit into field-appropriate caps so normal activity/research/support/career admissions detail is accepted while spam-length entries are rejected; structured profile descriptions remain at their existing 1000-1500 character caps. Docs updated: API contracts, decisions, and this task log. No admissions-outcome wording was added.
- [x] **V1-PRODUCT-POLISH-AND-FEATURES-001** Add controlled demo-readiness polish on top of the global unsaved-changes safeguards without starting a new product module. Frontend: application notes now use explicit Save/Cancel actions plus the existing unsaved-change dialog before switching tracked applications; recommendations have a real filter reset state, better empty-state CTAs, and expanded data context with deadline confidence plus official/supporting source notes when available; event catalog empty states now distinguish "no matching filters" from "nothing registered yet"; admin event moderation asks for confirmation before approve/reject/archive and can clear a stale rejection reason. Added 10 i18n keys across en/ru/uz-Latn/uz-Cyrl (1940 keys x 4). Validation passed locally: i18n parity, TypeScript, ESLint, and Next production build (27 routes). Backend was unchanged, so backend checks were not rerun. No XLSX upload/import/dry-run/execute, university data change, recommendation-logic change, or admissions-outcome wording was added.
- [x] **AUTH-NETWORK-STABILITY-001** Stop loading buttons/screens from getting stuck forever on slow or unreachable backend calls. A user reported registration stuck on "Please wait..." with no new Render logs — plausible given the shared API client's prior 60s timeout, which reads as "stuck forever" well before it resolves. Reduced the default request timeout from 60s to 20s (within the requested 15-25s range) and added a separate 12s timeout for the session check (`/auth/me/`), so a bounded, retryable error now surfaces quickly instead of a minute of silence. Added an `ApiError.errorCode` (`timeout`/`network`/`http`/`parse`) so screens can render a translated message instead of ever displaying the client's internal English fallback text — fixed two real call sites (`auth-form.tsx`, `onboarding-flow.tsx`) that were doing exactly that via `getApiErrorMessage`, which is the concrete i18n leak this bug produced. Login failures now show a stable localized "Invalid email or password" message instead of the backend's raw (English-only) validation text. The session-check "offline" gate gained a second "Back to login" action alongside Retry, wired to the existing `notifyAuthInvalid()` event (synchronous, no network call) rather than the network-dependent `logout()`, so it is instant even when the backend is genuinely unreachable — verified live in the preview by stubbing an unreachable backend and a stale token: the offline screen appeared within the new 12s window with both buttons, and "Back to login" returned to the login form immediately. Reviewed all named flows (login/register/me, onboarding save/complete, profile save, recommendations shortlist/track, event register/cancel, organizer save/submit, admin approve/reject/archive) for try/catch/finally coverage — all already reset their loading state correctly except recommendations' shortlist/track actions, which gained a per-item pending guard to prevent double-submission and double-network-calls during a slow request. No backend changes were needed. Validation passed locally: i18n parity (1943 keys x 4 locales), TypeScript, ESLint, Next production build (27 routes), and `git diff --check`. No recommendation logic, university data, or unsaved-change safeguards were touched, and no admission chance/probability/guarantee language was added.
- [x] **UI-DENSITY-SCROLL-POLISH-001** Fix founder-reported layout density and scroll issues: oversized dashboard, too much empty space, broken/double scrollbars, ugly sidebar scrollbar, exam timeline stuck on the left, cards not using available width. Root-caused before changing anything: the desktop shell was already architecturally correct (single `fixed inset-0` shell, sidebar and main content each independently `overflow-y-auto`, no body/html-level scroll) — the "ugly"/"double" scrollbar perception came from a styling mismatch, not a structural bug: only the sidebar nav region had the thin `.scrollbar-quiet` treatment, and it used the light-surface thumb color, which washes out to a pale sliver against the dark navy sidebar background; the main content region had no scrollbar styling at all, showing the browser's native (usually wide/gray) scrollbar next to the sidebar's thin one. Added a `.scrollbar-quiet-inverse` variant (navy-foreground-tinted thumb) for the sidebar and applied `.scrollbar-quiet` to the main content region so both independent, legitimate scroll surfaces now look consistent. Compacted density: shared `Card` default padding `p-5→p-4`, sidebar nav item padding/dividers tightened, header `h-16→h-14`, main content vertical padding reduced and made symmetric, dashboard hero section resized (smaller heading/padding), dashboard's explicit `p-5` Card overrides moved to `p-4`. Root-caused "exam timeline using only the left side" and "cards not using available width" as the same underlying bug: card grids used a fixed `sm:grid-cols-2 lg:grid-cols-3`-style template regardless of how many cards existed, so 1-2 cards (a typical exam countdown, a thin recommendation category, or an incomplete last row of dashboard module cards) rendered stuck in the leftmost column(s) with dead space on the right. A CSS Grid `auto-fit` attempt was tried and measured live against real production data, but grid solves this only when the *entire* grid has fewer items than one row — it does not fill a lone leftover item in the last row of a multi-row grid (grid-template-columns applies uniformly to every row). Switched instead to `flex flex-wrap` with `flex-1`/`min-w-[Xrem]` cards for the dashboard exam countdown, dashboard workspace modules, and the recommendations per-category grid, which correctly lets a lone last-row item grow to fill the full row. Verified live against the real production backend (logged in as the seeded demo student, `NEXT_PUBLIC_*_API_BASE_URL` temporarily pointed at Render, tokens injected into the preview browser) rather than guessing: measured that html/body have zero scroll at both 1440px and 390px (no page-level double scrollbar), zero horizontal overflow at both widths including the recommendations filter bar, and — concretely — that a lone 7th "Reach" recommendation card and a lone 5th dashboard module card each grew from a stuck ~362px/269px on the left to the full ~1110px row width after the flexbox change. Validation passed locally: i18n parity (1943 keys x 4, unchanged — no new user-facing strings), TypeScript, ESLint, Next production build (27 routes), and `git diff --check`. No backend changes, no recommendation/admissions logic changes, no university data touched, and no auth/onboarding/unsaved-change/timeout-handling safeguards were removed.
- [x] **ADMISSIONS-PROFILE-DEPTH-001** Finish the structured admissions-profile depth pass rather than rebuilding existing sections. Audit found the original 7 sections (activities, honors, olympiads, sports, research, essay drafts, portfolio projects) already existed end-to-end; the missing pieces were volunteering/community work, recommendation/counselor tracking, curated leadership activity categories, and connecting structured evidence to readiness/recommendations. Backend: added `Volunteer` and `Recommender` related models plus migration `user_profile_service.0006`, serializers, self-only viewsets, routes (`/api/profile/volunteering/`, `/api/profile/recommenders/`), CRUD tests, and granular readiness scoring for leadership, honors, olympiads, sports, research, portfolio, volunteering, recommenders, and structured essay drafts. Empty niche categories are surfaced as improvement nudges but do not drag down the star average unless evidence exists; structured profile entries also broaden university fit profile-depth evidence without per-university query churn. Frontend: profile page now loads, creates, edits, deletes, and persists volunteering and recommender entries using the existing compact `ProfileItemSection`; activity category is now a curated select with Leadership; activity forms expose hours/weeks/impact fields; the shared item section now has a true Close state and protects unsaved drafts when switching edits. i18n: profile-depth/readiness/component labels and new form labels are localized across en/ru/uz-Latn/uz-Cyrl, with readiness wording kept as evidence/readiness context rather than admissions-outcome language. Validation passed locally: Django check, `makemigrations --check --dry-run` (no changes; localhost Postgres history check warned but did not affect schema detection), SQLite `migrate --noinput`, full backend suite (298 tests), Ruff, i18n parity (1987 keys x 4), TypeScript, ESLint, Next production build (27 routes), and `git diff --check`. No XLSX upload/import/dry-run/execute, university data change, or admissions-outcome wording was added.
- [x] **PROFILE-DEPTH-QA-POLISH-001** Verify and polish the structured admissions-profile system after `b3d62c5`. Frontend: tightened the reusable profile item section for demo-readiness by switching explicit profile-item cards to compact `p-4`, making the header stack safely on narrow screens, adding an accessible delete label, improving empty-state guidance, giving long textareas a taller resizeable editing surface, showing textarea character counts against the existing 1000-1500 limits, and keeping Save/Cancel/Close behavior intact. Backend tests: added a full create/update/list/delete loop across all structured profile endpoints (activities, honors, olympiads, sports, research projects, essays, portfolio projects, volunteering, recommenders), added self-only update/delete coverage for recommenders, and added readiness tests for honors, sports, and removal of volunteering/recommender evidence. Existing recommendation query-count coverage still protects the fit/recommendation path from per-university query growth. Validation passed locally: targeted profile tests (40), Django check, `makemigrations --check --dry-run`, SQLite `migrate --noinput`, full backend suite (303 tests), Ruff, i18n parity (1988 keys x 4), TypeScript, ESLint, Next production build (27 routes), and `git diff --check`. Manual browser QA was attempted, but local hidden background server startup was unreliable in the shell host, so no browser interaction result is claimed. No onboarding expansion, university import, university data change, auth/onboarding gate removal, timeout/safeguard removal, or admissions-outcome wording was added.
- [x] **V1-DEMO-JOURNEY-POLISH-001** Make the student demo journey feel connected across onboarding, dashboard, profile, recommendations, events, and applications without adding a new module. Frontend: dashboard now shows a compact five-step journey (complete admissions profile, add exams, review recommendations, explore events, track applications) with a specific next-action CTA, profile gaps link directly to matching profile sections, empty dashboard widgets explain the next useful action, the profile page has section jump navigation plus completion indicators for activities/honors/olympiads/sports/research/volunteering/recommenders/essays/portfolio, and recommendations now explain which missing profile-depth signals reduce fit-estimate confidence. Events and applications empty states now point users toward the next workflow step. i18n parity passed with 2014 keys across four locales; TypeScript, ESLint, Next production build, and `git diff --check` passed. Backend was unchanged. No university import, university data change, auth/onboarding gate removal, timeout/safeguard removal, or admissions-outcome wording was added.
- [ ] **AI-001** Implement backend OpenRouter adapter, quotas, usage logs, safety policy, prompt-injection defenses, and provider fallback.
- [ ] **EXAM-001** Implement original question authoring/admin, practice sessions, answers, explanations, and scoring foundations.
- [ ] **FIN-001** Implement educational finance modules and quizzes with mandatory advice disclaimer.
- [ ] **SUB-001** Implement plan definitions, feature access rules, atomic usage counters, and monthly reset job.

## Recommended order

`FORMS-001 -> TICKETS-001 -> EVENTS-002 -> UNI-001 -> SUB-001 -> ROAD-001 -> ESSAY-001 -> AI-001 -> EXAM-001 -> FIN-001`

Next up: `AI-001` (or `OPPORTUNITY-MATCHING-001` / `MAJOR-CAREER-ORIENTATION-001` from the Phase B backlog).

`AUTH-002` is required before production launch and may run alongside later product slices.

## Next recommended product tasks

Recommended sequence:

`FORMS-001 -> TICKETS-001 -> TELEGRAM-001 -> EXPORT-001 -> ANTIABUSE-001 -> PAYMENTS-001 -> AUTH-002`

- [x] **ORGANIZER-001** Add organizer draft creation, ownership, submission, moderation, status logs, participant access, and localized role-aware UI.
- [x] **BETA-PREVIEW-001** Connect completed modules into a coherent beta shell, command-center dashboard, complete navigation, and localized preview pages.
- [x] **LOCAL-RUN-001** Add a founder-friendly SQLite backend launcher, frontend environment preparation, preview diagnostics, and a local troubleshooting guide.
- [x] **BETA-QA-001** Complete founder visual QA, remove duplicate navigation, improve role-guard recovery, and polish mobile overflow behavior.
- [x] **AUTH-GUARD-001** Require a backend-confirmed session before mounting any product shell or content, protect catalog reads, and distinguish offline from logout.
- [x] **V1-UI-REDESIGN-001** Consolidate the beta into a coherent V1 shell, dashboard, event-first workflow, honest preview modules, and real plan comparison.
- [x] **V1-BRAND-REDESIGN-002** Apply the ivory/navy/crimson academic identity, crisp geometry, restrained typography, and EduVerse favicon across the frontend.
- [x] **ONBOARDING-GATE-001** Require backend-confirmed onboarding, add the six-step profile foundation, rule-based major exploration, exam countdowns, layout polish, and clean preview build isolation.
- [x] **V1-ADMISSIONS-ONBOARDING-FINAL-001** Add the expanded major catalog, 40-question assessment, admissions proposals, recommended classes, target countries, and evidence-based readiness.
- [x] **V1-FINAL-POLISH-CONTINUE-001** Finish independent shell scrolling, compact profile layout, event map/filter hybrid, role navigation, organizer status summary, documentation, and final local checks.
- [x] **V1-DEMO-STABILIZATION-001** Stabilize founder demo auth/onboarding, normalize fresh profile payloads, seed role-specific local demo accounts, and document the founder review checklist.
- [x] **V1-FREEZE-HANDOFF-001** Freeze the current V1 beta state in `docs/V1_HANDOFF.md`; do not start a new feature before founder visual review.
- [ ] **FORMS-001** Standardize accessible form fields, validation summaries, and translated backend error mapping.
- [ ] **TICKETS-001** Model event registration/ticket intent without introducing payment processing.
- [ ] **TELEGRAM-001** Design an opt-in Telegram notification integration with privacy and rate-limit controls.
- [ ] **EXPORT-001** Add privacy-aware user data and roadmap exports.
- [ ] **ANTIABUSE-001** Add layered abuse controls for auth, event submissions, AI usage, and public forms.
- [ ] **PAYMENTS-001** Define provider-neutral payment intents and reconciliation after ticketing and abuse controls are ready.
